# Instagram Elements Checklist - Missing Items

**Based on Screenshot Analysis**

## ✅ Already Collected (13 elements)
- Main content divs
- Continue on web divs
- Empty container divs
- Content wrapper divs
- Text content divs
- Button divs
- Watch reel divs
- Span elements
- React components

---

## ❌ MISSING ELEMENTS (Need to Extract)

### 1. **Instagram Logo SVG** 🔴 HIGH PRIORITY
- **Location:** Top left of page
- **Element Type:** SVG with `aria-label="Instagram"`
- **Classes:** `x1lliihq x1n2onr6 x5n08af`
- **Attributes:** `fill="currentColor"`, `height="29"`, `role="img"`, `viewBox="32 4 11 ..."`
- **Container:** Inside `<div class="x1qjc9v5...">` (already have container, need SVG)
- **Action:** Extract from `sample.html` - search for `aria-label="Instagram"`

---

### 2. **Header Element** 🔴 HIGH PRIORITY
- **Element Type:** `<header>`
- **Classes:** `_ab15 xqt88ji x16v0oar x12m927z x1enc8s9 xhao81l x1a8lsjc x889kno`
- **Contains:** Logo container and "Continue on web" link
- **Action:** Extract from `sample.html` - search for `class="_ab15"`

---

### 3. **Video Player Container** 🔴 HIGH PRIORITY
- **Description:** Container with rounded corners for the vertical video
- **Aspect Ratio:** Vertical (9:16)
- **Features:** Rounded corners, black bars at top/bottom
- **Action:** Need to find video container divs in `sample.html`

---

### 4. **Video Text Overlay** 🔴 HIGH PRIORITY
- **Text Content:** "I will always choose you even on the days we don't understand each other."
- **Position:** Overlaid on video, slightly to the right
- **Style:** White text
- **Element Type:** Likely a `<div>` or `<span>` with absolute positioning
- **Action:** Search `sample.html` for this exact text

---

### 5. **Play Button Icon** 🔴 HIGH PRIORITY
- **Description:** White play button (right-pointing triangle) centered on video
- **Element Type:** Likely `<button>` or `<div>` with SVG icon
- **Position:** Centered on video player
- **Action:** Search for play button elements in `sample.html`

---

### 6. **Video Thumbnail/Image** 🟡 MEDIUM PRIORITY
- **Description:** The actual video thumbnail or image element
- **Element Type:** `<img>` or `<video>` with `poster` attribute
- **Action:** Extract video/image elements from `sample.html`

---

### 7. **Layout Wrapper Divs** 🟡 MEDIUM PRIORITY
- **Description:** Additional container divs for page layout structure
- **Examples:**
  - Main page wrapper
  - Content area containers
  - Video card wrapper
- **Action:** Extract layout structure from `sample.html`

---

## 🔍 Extraction Instructions

### To Find Missing Elements:

1. **Instagram Logo SVG:**
   ```bash
   grep -o '<svg[^>]*aria-label="Instagram"[^>]*>.*</svg>' sample.html
   ```

2. **Header Element:**
   ```bash
   grep -o '<header[^>]*class="_ab15[^"]*"[^>]*>.*</header>' sample.html
   ```

3. **Video Text Overlay:**
   ```bash
   grep -i "I will always choose you" sample.html
   ```

4. **Play Button:**
   ```bash
   grep -i "play\|button\|svg.*play" sample.html
   ```

5. **Video Container:**
   ```bash
   grep -i "video\|player\|reel" sample.html | head -20
   ```

---

## 📝 Notes

- The `sample.html` file contains the full Instagram page structure
- Some elements may be dynamically generated by React
- SVG logo path data might be truncated in HTML (need full viewBox and path)
- Video overlay text might be in a React component or data attribute

---

## ✅ Next Steps

1. Extract SVG logo from `sample.html`
2. Extract header structure
3. Find video container elements
4. Locate text overlay element
5. Extract play button element
6. Get video/image elements
7. Document layout wrapper structure

